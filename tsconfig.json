{
  // this is basic configuration
  // you can customize compilerOptions
  // but, it will not apply 100%
  // some compilerOptions may not apply
  "extends": "rocket-punch/tsconfig.base.json",
  "compilerOptions": {
    "baseUrl": "src"
  }
}
